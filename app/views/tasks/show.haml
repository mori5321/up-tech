%h1 ポップアップ(タスク) ※Ajaxでポップアップ的にするかも

.samp
  = @task.title
.samp
  = @task.kind
.samp
  = @task.slashed_start_datetime + "~" + @task.slashed_finish_datetime
.samp
  = link_to "完了", new_task_report_path(@task)
